<%= form_for(book) do |f| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
      <%= f.label :full_name %><br />
      <%= f.text_field :full_name, autofocus: true %>
  </div>
  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <h3>Outhors</h3>
   <div id='outhors'>
     <%= f.fields_for :authors do |author| %>
       <%= render 'author_fields', :f => author %>
     <% end %>
     <div class='links'>
       <%= link_to_add_association 'add auther', f, :authors %>
     </div>
   </div>
</div>
<div class="actions">
    <%= f.submit %>
  </div>
<% end %>
